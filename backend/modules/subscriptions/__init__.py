"""Subscriptions module — recurring user-scoped outflows.

Houses both `kind=subscription` (recurring charges from a counterparty) and
`kind=standing_instruction` (scheduled outbound transfers the user controls).
"""
